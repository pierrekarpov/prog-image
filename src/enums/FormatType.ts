export enum FormatType {
    JPEG = 1,
    PNG,
    GIF,
    BMP,
    TIFF,
}

export const FormatTypeList = [
    FormatType.JPEG,
    FormatType.PNG,
    // FormatType.GIF, //NOTE: not support yet
    FormatType.BMP,
    // FormatType.TIFF, //NOTE: not support yet
]